# Working Patterns — retired into the constitution (2026-07-12)

This file's content migrated at the working-system overhaul:

- Collaboration patterns → `CLAUDE.md` (Judgment principles).
- SDK/API engineering patterns → [`.claude/rules/api-design.md`](rules/api-design.md)
  (path-scoped, loads when touching Kotlin).
- Operating stance for YouTrack/board → `CLAUDE.md` (Work Loop + pointer table).

Full pre-migration text: git history of this path (last full version at tag-adjacent
commit `c5a313e` and earlier). Do not grow this file again — new patterns go to their
home per the constitution's rule ladder (hook → structure → prose-line-in-constitution).
