# Known Pitfalls — retired into the constitution (2026-07-12)

This file's content migrated at the working-system overhaul:

- Recurring mechanical pitfalls → hooks + CI (branch names, private content, hook
  regression tests) — deterministic, not prose.
- Code-pattern pitfalls (`field` param, K/N↔Cocoa weak delegates) →
  [`.claude/rules/api-design.md`](rules/api-design.md).
- Process pitfalls (premature "ready", derived-source drift, release-before-reality)
  → `CLAUDE.md` (Judgment principles) and the release-gate skill.

Full pre-migration text (26 entries with their incident stories): git history of this
path. A new pitfall is a system defect — capture as an issue and walk the ladder
(hook → structure → accept); it does not get appended here.
