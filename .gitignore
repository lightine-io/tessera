# Operating system files
.DS_Store
Thumbs.db
desktop.ini
*~

# IDE files
.idea/
*.iml
*.iws
.vscode/
*.swp
*.swo

# JetBrains-specific runtime files (keep shared run configurations if added later)
.idea/workspace.xml
.idea/tasks.xml
.idea/usage.statistics.xml
.idea/shelf/
.idea/dictionaries/
.idea/httpRequests/
.idea/dataSources/

# Build outputs
build/
out/
target/
.gradle/
*.class

# iOS / Xcode build outputs
DerivedData/
*.xcuserdata/
*.xcworkspace/xcuserdata/
Pods/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
xcuserdata/
xcshareddata/

# Kotlin / KMP-specific
local.properties
.kotlin/
kotlin-js-store/

# Gradle wrapper bytecode (the wrapper jar IS committed; these are build artifacts)
.gradle/

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Session handoff files live in .handoffs/ (working notes from individual Claude Code sessions,
# not project history). The leading slash anchors the match to project root so a nested
# .handoffs/ elsewhere — should one ever appear — is not silently excluded too.
/.handoffs/

# Code/doc alignment recap files live in .recaps/ (snapshot audits, not project history).
# Same leading-slash rationale as the handoffs entry above.
/.recaps/

# Conformance verification working notes live in .conformance/ (per-session findings from
# spec-vs-implementation verification passes). Same rationale as the recap files — these are
# working artifacts, not committed project history. Leading slash for the same reason.
/.conformance/

# Review & decision-logs live in .reviews/ (living decision records from working sessions — reasoning
# behind structural and collaboration decisions, candid self-critique, and credential-rotation
# discussion; backward-looking, present-state, and forward decisions braided together). Working
# notes, not committed project history; some content is deliberately not for the public repo. Same
# leading-slash rationale as the entries above.
/.reviews/

# Discovery docs live in .discovery/ (structured tool/approach investigations that precede a decision —
# research + capability mapping + gaps/solutions, with sources). Working notes, not committed project
# history; some migrate into other homes (e.g. a future YouTrack KB) once acted on. Same leading-slash
# rationale as the entries above.
/.discovery/

# Plans live in .plans/ (execution/action plans derived from reviews — the bridge that graduates
# decisions into tasks; the filename CATEGORY prefix specializes the type, e.g. ACTION-PLAN-*, PLAN-*,
# MIGRATION-PLAN-*). Working notes, not committed project history; transient — they dissolve into the
# real homes (rules / tasks / a future YouTrack) once acted on. Same leading-slash rationale as above.
/.plans/

# Local reference copies of external specifications live in .spec/ (e.g., ICAO Doc 9303 PDFs
# downloaded for implementation verification). These are NEVER committed: ICAO's terms restrict
# redistribution of their materials without written permission. The spec content is consulted
# locally to verify the SDK's implementations against authoritative source; findings translate
# into code changes and citations via section numbers, never via verbatim spec content. Leading
# slash anchors to project root (avoid accidental matches in nested module trees).
/.spec/

# Private-content scan terms file lives at .claude/private-content-terms.local (per-contributor
# local-only list of substrings that must never appear in committed files — organizational and
# location identifiers). Read by scripts/private-content-scan.sh, which is wired into the
# PreToolUse hook in .claude/settings.json to block any git push that would leak them. The
# terms themselves are the private context being protected, so the file is gitignored by design.
/.claude/private-content-terms.local

# Per-machine runtime lock written by the scheduled-tasks tooling. Transient local state, never
# tracked — analogous to any .lock file.
/.claude/scheduled_tasks.lock

# Coverage reports
coverage/
*.lcov
.nyc_output/

# Dependency directories (if any per-module tools introduce them)
node_modules/

# Temporary files
*.tmp
*.temp
.cache/
